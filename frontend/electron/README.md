# Electron Setup for Windows

## Development
```bash
npm run electron:dev
```

## Build Windows Installer
```bash
npm run electron:build:win
```

Output will be in `dist-electron/` folder.

## Icon
Place your app icon as `icon.ico` in the `electron/` folder.

